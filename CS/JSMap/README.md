# Interactive Map

JavaScript-based interactive map for visualizing alpine lake monitoring data on the Lacs Sentinelles website.

## Usage

Open `index.html` in a browser or deploy to web server.

## Dependencies

- Leaflet.js (or Mapbox/OpenLayers — adjust as needed)

## Data

Reads from image/GeoJSON/CSV files containing lake coordinates and observation data (available on Zenodo).